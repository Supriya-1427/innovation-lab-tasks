Quiz Answer

Q: What does Math.floor(4.7) return?
A: 4
1. How to convert a string to uppercase?
str.toUpperCase()


2. Difference between forEach and map?

forEach: Executes a function on each element (no return).

map: Returns a new array after applying function.



3. What does for-in iterate over in arrays?
Indexes (keys).


4. Predict output of .map() on array
Applies function to each item, returns new array.
